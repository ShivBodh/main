// This component has been temporarily removed to resolve a build-time dependency issue
// with the `html-to-image` package, which was preventing deployments.
// The feature will be re-implemented with a more robust, server-safe solution.

export default function PanchangaWidgetsClient() {
  return null;
}
