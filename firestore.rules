rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read for the calendar and public write for the scraper.
    // In a production app, write access should be restricted.
    match /media/{documentId} {
      allow read, write: if true;
    }

    // Any other collection is denied by default.
  }
}
