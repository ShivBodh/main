rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access only to the 'media' collection.
    // This is for development and will be secured before launch.
    match /media/{docId} {
      allow read, write: if true;
    }
  }
}
