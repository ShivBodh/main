// This component has been removed to avoid confusion.
// The results of the content processing script are now viewed directly on the
// Bodha Calendar page after running `npm run scrape`.
export default function ScraperResultClient() {
    return null;
}
